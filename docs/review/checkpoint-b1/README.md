# Checkpoint B1 visual review

These deterministic screenshots are generated by `apps/web/e2e/checkpoint-b1.spec.ts` using the synthetic H.264 MP4 fixture only:

- `desktop-evidence-instructions.png`
- `mobile-evidence-instructions.png`
- `desktop-evidence-ready.png`
- `mobile-evidence-ready.png`

Regenerate them from Git Bash with:

```bash
export DATABASE_URL="postgresql://nextstep:nextstep@localhost:5433/nextstep?schema=public"
docker compose up -d minio minio-init
corepack pnpm test:e2e:checkpoint-b1
```

The test checks serious/critical axe findings, mobile horizontal overflow, strict browser console errors outside the deliberate interrupted-upload window, direct private upload and playback, consent sequencing, cross-household denial, and worker-driven deletion.
